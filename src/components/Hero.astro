---
import Icon from './Icon.astro';
import { escapeHtml } from '../lib/text';
import type { Resume } from '../data/schema';

interface Props {
  profile: Resume['profile'];
  linkedin: string | null;
}

const { profile, linkedin } = Astro.props;

// The headline allows one emphasized phrase wrapped in asterisks.
const headlineHtml = escapeHtml(profile.headline).replace(
  /\*([^*]+)\*/,
  '<em class="hero-mark">$1</em>'
);

---

<section class="hero" aria-labelledby="hero-title">
  <div class="hero-copy reveal">
    <p class="eyebrow"><span class="status-dot" aria-hidden="true"></span> {profile.role} at {profile.company}</p>
    <h1 id="hero-title" set:html={headlineHtml} />
    <div class="hero-intro-group">
      <svg class="systems-flow" viewBox="0 0 42 56" aria-hidden="true" focusable="false">
        <path d="M5 8h12c7 0 7 10 14 10h6M5 28h32M5 48h12c7 0 7-10 14-10h6" />
        <circle cx="5" cy="8" r="2.5" />
        <circle cx="5" cy="28" r="2.5" />
        <circle cx="5" cy="48" r="2.5" />
        <circle cx="37" cy="18" r="2.5" />
        <circle cx="37" cy="28" r="2.5" />
        <circle cx="37" cy="38" r="2.5" />
      </svg>
      <p class="hero-intro">{profile.intro}</p>
    </div>
    <div class="hero-actions">
      <a class="button button-primary" href="#work">
        Explore my work <Icon name="arrow-down-right" size={16} />
      </a>
      {
        linkedin && (
          <a class="button button-secondary" href={linkedin} target="_blank" rel="noreferrer">
            Connect on LinkedIn <Icon name="arrow-up-right" size={16} />
          </a>
        )
      }
    </div>
  </div>

  <div class="hero-visual reveal" data-delay="1">
    <div class="portrait-frame">
      <img
        src={profile.avatar}
        alt={`Portrait of ${profile.name}`}
        width="480"
        height="600"
        loading="eager"
        decoding="async"
      />
    </div>
    <p class="portrait-note"><span>Based in</span> {profile.location}</p>
  </div>

</section>

<style>
  .hero {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1.4fr) minmax(300px, 0.6fr);
    grid-template-areas: 'copy visual';
    align-items: center;
    gap: clamp(36px, 5vw, 68px);
    width: var(--content);
    min-height: min(620px, calc(100svh - 120px));
    margin: 0 auto;
    padding: clamp(24px, 3vh, 40px) 0 20px;
  }

  .hero-copy {
    grid-area: copy;
    padding-bottom: 12px;
  }

  .hero .eyebrow {
    margin-bottom: 22px;
    color: var(--ink-soft);
  }

  .status-dot {
    display: inline-block;
    width: 7px;
    height: 7px;
    margin-right: 8px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 4px var(--accent-tint);
  }

  .hero h1 {
    max-width: 17ch;
    font-family: var(--display);
    font-size: clamp(38px, 4.4vw, 58px);
    font-weight: 680;
    line-height: 1.08;
    letter-spacing: -0.028em;
  }

  .hero h1 :global(.hero-mark) {
    position: relative;
    font-style: normal;
    color: var(--accent);
  }

  .hero-intro-group {
    position: relative;
    max-width: 62ch;
    margin-top: 20px;
  }

  .systems-flow {
    position: absolute;
    top: 50%;
    left: -58px;
    width: 42px;
    height: 56px;
    transform: translateY(-50%);
    color: var(--accent);
    fill: var(--paper);
    stroke: currentColor;
    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .hero-intro {
    color: var(--ink-soft);
    font-size: clamp(16px, 1.4vw, 19px);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 24px;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    min-height: 46px;
    padding: 0 22px;
    border: 1px solid transparent;
    border-radius: 999px;
    font-weight: 560;
    text-decoration: none;
    transition: background 0.18s ease, color 0.18s ease, border-color 0.18s ease,
      box-shadow 0.18s ease, transform 0.18s ease;
  }

  .button-primary {
    background: var(--ink);
    color: #fff;
    box-shadow: var(--shadow-sm);
  }

  .button-primary:hover {
    background: var(--accent);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .button-secondary {
    border-color: var(--line-strong);
    background: var(--paper);
  }

  .button-secondary:hover {
    border-color: var(--ink);
    transform: translateY(-2px);
    box-shadow: var(--shadow-sm);
  }

  .hero-visual {
    grid-area: visual;
    position: relative;
    align-self: center;
    width: min(100%, 350px);
    justify-self: end;
  }

  .portrait-frame {
    position: relative;
    aspect-ratio: 4 / 5;
    border-radius: var(--radius-lg);
    background: var(--panel);
    box-shadow: var(--shadow-md);
    overflow: hidden;
  }

  .portrait-frame::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, transparent 55%, rgba(11, 12, 14, 0.28));
    pointer-events: none;
  }

  .portrait-frame img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 50% 22%;
    filter: saturate(0.85) contrast(1.02);
  }

  .portrait-note {
    margin-top: 18px;
    font-family: var(--sans);
    font-size: 13px;
    color: var(--ink-soft);
  }

  .portrait-note span {
    color: var(--ink-faint);
    margin-right: 6px;
  }

  @media (min-width: 1800px) {
    .hero {
      grid-template-columns: minmax(0, 1.35fr) minmax(440px, 0.65fr);
      gap: clamp(80px, 6vw, 160px);
      min-height: min(1040px, calc(100svh - 112px));
      padding: clamp(52px, 5vh, 84px) 0 36px;
    }

    .hero .eyebrow {
      margin-bottom: 30px;
      font-size: 14px;
    }

    .hero h1 {
      max-width: 15ch;
      font-size: clamp(96px, 3vw, 116px);
    }

    .hero-intro-group {
      max-width: 66ch;
      margin-top: 30px;
    }

    .hero-intro {
      font-size: 22px;
    }

    .hero-actions {
      margin-top: 34px;
    }

    .button {
      min-height: 52px;
      padding: 0 24px;
      font-size: 17px;
    }

    .hero-visual {
      width: min(100%, 520px);
    }

    .portrait-note {
      font-size: 14px;
    }

  }

  @media (max-width: 900px) {
    .hero {
      grid-template-columns: 1fr;
      grid-template-areas: 'copy' 'visual';
      min-height: auto;
      max-height: none;
      padding-top: 56px;
    }

    .hero-visual {
      display: grid;
      grid-template-columns: minmax(0, 420px);
      justify-content: start;
      width: 100%;
      justify-self: start;
    }

  }

  @media (max-width: 620px) {
    .hero {
      gap: 28px;
      padding-top: 40px;
    }

    .hero h1 {
      max-width: 15ch;
      font-size: 42px;
      line-height: 1.03;
    }

    .hero-actions,
    .hero-actions .button {
      width: 100%;
    }

    .hero .eyebrow {
      margin-bottom: 18px;
    }

    .hero-intro-group {
      margin-top: 18px;
    }

    .systems-flow {
      position: static;
      width: 34px;
      height: 48px;
      margin-bottom: 10px;
      transform: none;
    }

    .hero-intro {
      font-size: 16px;
      line-height: 1.55;
    }

    .hero-actions {
      margin-top: 20px;
    }

    .hero-visual {
      grid-template-columns: minmax(0, 360px);
    }
  }
</style>
